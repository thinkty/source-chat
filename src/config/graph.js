/**
 * This module contains the configuration of the graph such as the node types
 */

const INTENT_TYPE = 'intentNode';
const CONTEXT_TYPE = 'contextNode';
const expectedNodeTypes = [INTENT_TYPE, CONTEXT_TYPE];

module.exports = {
  INTENT_TYPE,
  CONTEXT_TYPE,
  expectedNodeTypes,
};
